// [A]
const doTheFirstThing = (params) => {
  // do the first thing
  return undefined;
}

// [A]
const doTheSecondThing = (params) => {
  // do the second thing
  return undefined;
}

// [A]
const doTheThirdThing = (params) => {
  // do the third thing
  return undefined;
}

// [A]
const doTheFourthThing = (params) => {
  // do the fourth thing
  return undefined;
}